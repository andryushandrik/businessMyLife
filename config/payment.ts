// Don't change orders below
export enum PaymentStatuses {
	SUCCESS = 'Success',
	PENDING = 'Pending',
	REJECTED = 'Rejected',
}
export enum PaymentMethods {
	INTERNAL = 'Internal payment with balance',
	EXTERNAL = 'External payment with bank',
}
