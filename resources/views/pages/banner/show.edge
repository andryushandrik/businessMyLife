@layout('layouts/index')

@set('title', `Баннер - ${item.title}`)

@section('styles')
  <link href="/css/banner.css" rel="stylesheet">
  <link href="/vendor/summernote/summernote.css" rel="stylesheet">
@endsection

@section('body')
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">
        <h4 class="card-title">{{ title }}</h4>
        <a href="{{ route('banners.index') }}" class="btn btn-primary btn-sm">Назад</a>
      </div>
      <div class="card-body">
        <div class="basic-form">

          <div class="form-group">
            <label class="mb-1">Название</label>
            <input type="string" class="form-control" value="{{ item.title }}" disabled>
          </div>

          <div class="form-group">
            <label class="mb-1"><strong>Описание</strong></label>
            <textarea id="summernote" class="summernote" disabled>{{ item.description }}</textarea>
          </div>

          <div class="form-group mb-3">
            <label class="mb-1">Изображение</label>
            <br>
            <img src="{{ getMedia(item.image) }}" class="news_image" style="max-width: 100%" alt="">
          </div>

          <div class="form-group">
            <label class="mb-1">Ссылка</label>
            <input type="string" class="form-control" value="{{ item.link ?? '' }}" disabled>
          </div>

        </div>
      </div>
    </div>
  </div>
@endsection

@section('scripts')
  <script src="/vendor/summernote/js/summernote.min.js"></script>
  <script src="/js/plugins-init/summernote-init.js"></script>
@endsection
